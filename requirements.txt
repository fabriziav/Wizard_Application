# Project Python dependencies
# NOTE: sensitive configuration (passwords, keys, connection strings, etc.)
# must NOT be stored in this file. See .env.example for placeholders and use
# a local .env (ignored by VCS) or a secrets manager in production.

# Avoid pinning to known-vulnerable versions. Exclude the vulnerable release
# and pick a patched version in your environment. Change the constraint to a
# specific patched version when your project has been tested with it.
bentoml!=1.4.0  # exclude known vulnerable 1.4.0 release; upgrade to patched version

# Correct package specification for requests. Use a reasonable range and
# adjust to the exact version your project supports.
requests>=2.0.0,<3.0.0

# If you need to record additional Python deps, add them here, one per line.
# Secrets and environment-specific configuration should go in .env (not checked
# in) and .env.example (checked in with SAMPLE values). See .env.example.
